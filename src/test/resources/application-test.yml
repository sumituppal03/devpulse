spring:
  datasource:
    url: jdbc:h2:mem:devpulse_test;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379

github:
  token: test-token
  webhook-secret: test-webhook-secret

ollama:
  base-url: http://localhost:11434
  model-name: llama3.2

groq:
  api-key: test-key
  model-name: llama-3.3-70b-versatile